# Deprem App server environment variables example
# Copy this to .env and fill values for your environment

# Server
PORT=3000
NODE_ENV=development

# APNs (iOS) - optional, required if you want server to send APNs pushes
# APNS_KEY_PATH should point to your .p8 key file on the server
APNS_KEY_PATH=./keys/AuthKey_XXXXXXXXXX.p8
APNS_KEY_ID=XXXXXXXXXX
APNS_TEAM_ID=YYYYYYYYYY
APNS_BUNDLE_ID=com.yourorg.depremapp

# MQTT broker for Android persistent pushes (use mqtts:// for TLS)
MQTT_BROKER_URL=mqtts://broker.example.com:8883
MQTT_USERNAME=your_mqtt_user
MQTT_PASSWORD=your_mqtt_password

# (Optional) Database/other credentials
MONGODB_URI=mongodb://user:pass@localhost:27017/deprem

# Security notes:
# - Keep your APNs .p8 key private and never commit to git
# - Use TLS (mqtts://) for MQTT broker in production
# - Protect the server .env file and restrict access to the VDS
